# Local dev — proxy API/storage through Vite (no CORS). Match your Herd/Valet site:
VITE_API_PROXY=http://potandleaf-backend.test
# php artisan serve alternative:
# VITE_API_PROXY=http://localhost:8000

# Do NOT set VITE_API_URL here — that is for production builds only (.env.production).
